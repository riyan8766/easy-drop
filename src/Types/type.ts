export interface CardProps {
  icon: React.ReactNode;
  title: string;
  description: string;
  className?: string;
}

export interface counterProps {
  icon: React.ReactNode;
  title: string;
  description: string;
}